beautifulsoup4
# Security: keep pinned to a patched version (pip-audit blocking in CI).
pdfminer.six==20251230
tqdm~=4.66.0
defusedxml>=0.7.1  # Secure XML parsing to prevent XXE attacks
typer>=0.9.0  # Modern CLI framework with type hints
rich>=13.0.0  # Used by Typer for enhanced output formatting
requests>=2.31.0  # Required for OpenAI-compatible multimodal image detection

# Note:
# - This file contains *minimal runtime* dependencies.
# - For development (tests) and optional features (NER engines, extra file formats),
#   use `requirements-dev.txt` or install extras via `pip install -e ".[dev]"`.

"""REST API module for the PBD Toolkit.

Provides a FastAPI-based HTTP interface for triggering scans, querying
results and consuming analytics data.  Requires the ``api`` extra::

    pip install -e ".[api]"
"""
